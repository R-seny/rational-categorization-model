### Whether to use strong or weak sampling to obtain initial sampling probabilities.
### Strong sampling - pick class first, then randomly pick example from this class.
### Weak sampling - pick example randomly, then label it correctly.
STRONG_SAMPLING_PRIOR = True